export default getTimeCharacteristics = ( referenceTimeStampUTC,referenceTimestampOffsetMinutes ) => {


  const hour = convertedDate.getHours()
  const minutes = convertedDate.getMinutes()

  return {
    hour,
    paddedHour,
    minutes,
    paddedMinutes,
    meridianString
  }
   

}